class Solution {
    int count(int[] arr, int n, int x) {
        // code here
        int c=0,l=0,h=n-1;
        while(l<=h)
        {
            int mid=(l+h)/2;
            if(arr[mid]==x)
             
        }
        return c;
    }
}
